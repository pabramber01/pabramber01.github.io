export type TableOfContent = {
  text: string;
  link: string;
};

export type CareerCard = {
  title: string;
  company: string;
  description: string;
  image: string;
  periodStart: string;
  periodEnd?: string;
};

export type WorkCard = {
  image: string;
  fit: string;
  title: string;
  description: string;
  link: string;
};
